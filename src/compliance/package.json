{
  "name": "@parserddd/compliance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic German/EU tachograph compliance engine for normalized DDD timelines.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp -r schemas dist/schemas",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}
